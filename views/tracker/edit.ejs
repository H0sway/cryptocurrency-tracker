<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Track Your Crypto Investments</title>
  <% include ../partials/head %>
</head>
<body>

  <% include ../partials/header %>

  <h1>Edit Your Investment</h1>
  <h3>Type in the amount you've invested below. We'll save it for you</h3>

  <div class="links">
    <div class="link">
      <a href="/tracker/tracker">Your Crypto Tracker</a>
    </div>
    <div class="link">
      <a href="/tracker/tracker/<%= currency.id %>">Back To Currency</a>
    </div>
  </div>

  <div class="edit">
    <form action="<%= `/tracker/tracker/${currency.id}?_method=PUT` %>" method="POST">

      <div class="editflex">
        <label for="amount">Amount Invested:</label>
        <input id="amount" type="number" step="any" name="amount" value="<%= investment.amount %>" />
      </div>

      <div class="editflex">
        <input type="submit" value="Finished" />
      </div>

    </form>
  </div>
</body>
</html>
